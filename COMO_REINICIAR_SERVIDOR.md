# 🔄 Como Reiniciar o Servidor

## Passo 1: Parar o Servidor

Se o servidor estiver rodando, você verá algo assim no terminal:

```
VITE v5.x.x  ready in xxx ms

➜  Local:   http://localhost:8080/
```

**Para parar o servidor:**

1. Clique no terminal onde o servidor está rodando
2. Pressione **`Ctrl + C`** (segure Ctrl e pressione a tecla C)
3. Você verá uma mensagem como: `^C` e o servidor será parado

## Passo 2: Iniciar o Servidor Novamente

Após parar o servidor, execute:

```bash
npm run dev
```

O servidor iniciará e você verá:

```
VITE v5.x.x  ready in xxx ms

➜  Local:   http://localhost:8080/
➜  Network: use --host to expose
```

## ⚠️ Se Você Não Tem o Servidor Rodando

Se você **não** tem um servidor rodando, apenas execute:

```bash
npm run dev
```

Isso iniciará o servidor pela primeira vez.

## 📍 Onde Executar?

Execute os comandos no terminal na pasta do projeto:
```
C:\Users\bruno\Downloads\trim-squire-main\trim-squire-main
```

## 🔍 Verificar se Está Funcionando

Após iniciar, acesse no navegador:
**http://localhost:8080**

Se tudo estiver correto, você verá a página inicial do Barber!

