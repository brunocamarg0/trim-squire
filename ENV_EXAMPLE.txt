# Firebase Configuration
# Get these values from https://console.firebase.google.com

VITE_FIREBASE_API_KEY=your-api-key-here
VITE_FIREBASE_AUTH_DOMAIN=your-project-id.firebaseapp.com
VITE_FIREBASE_PROJECT_ID=your-project-id
VITE_FIREBASE_STORAGE_BUCKET=your-project-id.appspot.com
VITE_FIREBASE_MESSAGING_SENDER_ID=your-messaging-sender-id
VITE_FIREBASE_APP_ID=your-app-id

# INSTRUCTIONS:
# 1. Create a file named .env.local in the root of the project
# 2. Copy the content above and replace with your actual Firebase credentials
# 3. Get your credentials from: https://console.firebase.google.com
# 4. DO NOT commit .env.local to version control (it's already in .gitignore)

